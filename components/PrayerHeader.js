import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const PrayerHeader = () => {
  return (
    <View>
      <Text>prayerHeader</Text>
    </View>
  )
}

export default PrayerHeader;

const styles = StyleSheet.create({})